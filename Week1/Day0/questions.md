# Questions For Future Learning

## PowerShell

1. How exactly does the PowerShell pipeline work?
2. Why does `$_` represent the current object?
3. How does `ForEach-Object` process data internally?

## GitHub

4. How does GitHub CLI authenticate securely?
5. What happens behind the scenes during `git push`?

## Operating Systems

6. Difference between:

   * CMD
   * PowerShell
   * Bash

7. Why does Linux support brace expansion but PowerShell does not?

## Productivity

8. How can PowerShell scripts automate repository creation?
9. How can recurring Summer Analytics tasks be automated?
